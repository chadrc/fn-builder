{
  "name": "fn-builder",
  "version": "0.0.1",
  "description": "Function composition library.",
  "main": "index.js",
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "clean": "rm -rf packages/*/*.js packages/*/*.js.map packages/*/*.d.ts"
  },
  "author": "Chad R Collins",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.4.3"
  }
}
